CREATE TABLE messages (  -- Create a new table named 'messages'
    id INT AUTO_INCREMENT PRIMARY KEY,  -- Define a column 'id' as an integer with auto-incrementing values, and set it as the primary key for the table
    message TEXT  -- Define a column 'message' to store text data
);
